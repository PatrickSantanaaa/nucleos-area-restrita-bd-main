/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { RetiradaPatrocinioService } from './retirada-patrocinio.service';

describe('Service: RetiradaPatrocinio', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [RetiradaPatrocinioService]
    });
  });

  it('should ...', inject([RetiradaPatrocinioService], (service: RetiradaPatrocinioService) => {
    expect(service).toBeTruthy();
  }));
});
