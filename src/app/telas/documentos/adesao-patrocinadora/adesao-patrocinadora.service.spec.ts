/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { AdesaoPatrocinadoraService } from './adesao-patrocinadora.service';

describe('Service: AdesaoPatrocinadora', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [AdesaoPatrocinadoraService]
    });
  });

  it('should ...', inject([AdesaoPatrocinadoraService], (service: AdesaoPatrocinadoraService) => {
    expect(service).toBeTruthy();
  }));
});
